# Review: the block message names the exact tool calls

One fresh-context round on a two-file diff (`plugins/forge-guard/hooks/protected.py`
message text, `plugins/forge-guard/verify.sh` assertions). Verdict: APPROVE WITH
NITS, both nits folded in before recording this review: the fallback sentence
now covers a synchronous launch tool ("that return IS the wait"), and the
verifier asserts the blocking-wait invocation appears in BOTH branches of the
message, not merely somewhere.

The reviewer verified: content parity with the old message (nothing lost —
fingerprint path, `## Prompt` requirement, no-priming warning, expiry
explanation all present); Python correctness of the concatenation (only two
f-string pieces, no brace hazards); the four verifier assertions match the
emitted stderr byte-for-byte with the run green at 20/20; no behavior change
beyond message text (fingerprinting, release valve, exit codes untouched); and
that `uv run forge check`'s single FAIL is the hook-parity drift rule doing its
designed job on an unpushed guard edit.

## Prompt

Verbatim prompt given to the reviewer:

> You are a fresh-context reviewer for the git repository at /tmp/forge-work.
> You have not seen any conversation about this change. Judge it adversarially
> on its own merits.
> The uncommitted diff touches exactly two files:
> plugins/forge-guard/hooks/protected.py (the Stop-hook guard: its block
> message was rewritten) and plugins/forge-guard/verify.sh (its verifier:
> assertions updated to match).
> Steps: git diff both files; read enough surrounding code to judge them (the
> whole protected.py at least). Evaluate: the new block message - it now names
> exact tool invocations (Agent(prompt=...), TaskOutput(task_id=..., block=true,
> timeout=600000)) in numbered order, including the "reviewer already running"
> case. Is it unambiguous? Could a session misread it (copy the placeholder
> text literally, pick the wrong branch of 1 vs 2, or believe TaskOutput exists
> when it might not)? Does the fallback sentence keep it portable? Does the
> message still carry everything the old one did (fingerprint path, ## Prompt
> requirement, no-priming warning, review-expiry explanation)? Anything lost?
> Python correctness of the edited f-string block. verify.sh: do the four new
> assertions match substrings of the new message byte-for-byte? Run it and
> confirm 20 passed, 0 failed. Would each assertion fail if its message line
> were deleted? Any behavior change beyond the message text? Note that forge
> check shows one FAIL: the hook-parity drift rule, because the guard's tree
> copy now differs from origin/main - the designed state for an unpushed guard
> edit. Confirm it is the only failure.
> Return markdown: Summary, Findings (each with severity), Verdict.
